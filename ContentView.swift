import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Image(systemName: "power")
                .imageScale(.large)
                .foregroundColor(Color.brown)
            Text("Power iOS Back On To Run App")
        }
    }
}
